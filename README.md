# Gatsby v2 WordPress

```
"scripts": {
    "build": "gatsby build",
    "dev": "gatsby develop",
    "start": "npm run develop",
    "format": "prettier --write \"src/**/*.js\"",
    "precommit": "lint-staged"
}
```